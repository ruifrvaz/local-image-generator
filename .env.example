# Local Image Generator - Environment Configuration Template
# Copy this file to .env and update values for your environment
# Traceability: STK-CONFIG-008, STK-CONFIG-016

# ===========================
# Backend Configuration
# ===========================

# Backend server host (0.0.0.0 to accept all connections)
BACKEND_HOST=0.0.0.0

# Backend server port
BACKEND_PORT=8000

# Frontend CORS origins (comma-separated list)
# Update WSL IP address (172.31.x.x) to match your system
# To find WSL IP: run `hostname -I` in WSL terminal
CORS_ORIGINS=http://localhost:5173,http://172.31.243.212:5173

# ComfyUI API URL
# Update if ComfyUI runs on different host/port
COMFYUI_API_URL=http://localhost:8188

# Gallery storage path for generated images
# Update to match your desired storage location
# Example: /home/username/images/outputs or /mnt/c/Users/YourName/images/outputs
GALLERY_STORAGE_PATH=/home/user/images/outputs

# ===========================
# Frontend Configuration
# ===========================

# Backend API base URL (must use VITE_ prefix for Vite)
# Update WSL IP address to match CORS_ORIGINS
VITE_API_BASE_URL=http://172.31.243.212:8000

# ===========================
# Environment-Specific Notes
# ===========================

# Local Development (WSL2):
# - Find WSL IP with: hostname -I
# - Update all IP addresses (172.31.x.x) to match
# - Both CORS_ORIGINS and VITE_API_BASE_URL must use same IP
#
# Alternative (Port Forwarding):
# - Use localhost everywhere instead of WSL IP
# - Requires WSL port forwarding to be configured
# - CORS_ORIGINS=http://localhost:5173
# - VITE_API_BASE_URL=http://localhost:8000
#
# Production/Remote:
# - Replace localhost/WSL IPs with actual server IPs/domains
# - Use HTTPS URLs when deploying with SSL/TLS
# - Consider using environment variables in deployment platform
